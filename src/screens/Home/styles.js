import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        width: '100%',  
        alignItems:"center",     
     },
     titulo:{
        fontSize: 20,
        fontWeight: 'bold',       
        marginTop:10,     
    },     
});

export default styles;
